import { PromotionPipe } from './promotion-pipe.pipe';

describe('PromotionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionPipe();
    expect(pipe).toBeTruthy();
  });
});
